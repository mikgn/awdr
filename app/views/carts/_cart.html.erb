<article>
  <h2>Your Cart</h2>
  <table>
    <%= render(cart.line_items.sort) %>
    <tfoot>
      <tr>
        <th colspan="2">Total:</th>
        <th class="price"><%= number_to_currency(@cart.total_price) %></th>
      </tr>
    </tfoot>
  </table>
<!--   <%= button_to 'Empty cart', @cart, class: 'empty-cart-btn',
                                     method: :delete,
                                     remote: true,
                                     data: { confirm: 'Are you sure?' } %> -->

  <%= button_to 'Checkout', new_order_path, class: "checkout",
                                            method: :get %>
</article>
